# CPU-only PyTorch build; do not install a CUDA wheel - the harness contains
# no CUDA code paths and a CUDA build would change nothing but download size.
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch==2.9.0
numpy==2.2.4
psutil==7.0.0
pytest==9.1.1
